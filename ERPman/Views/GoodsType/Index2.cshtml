@model Basicinfo.Application.Contract.GoodsType.SelectGoodsTypeQuery

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm("Create", "Goodstype", FormMethod.Post))
{
    @Html.AntiForgeryToken()
    <table>
        <tr>
            <td>
                @Html.TextBoxFor(model => model.Id);
            </td>
            <td>
                @Html.TextBoxFor(model => model.TypeTitle);
            </td>
            <td>
                @Html.TextBoxFor(model => model.ParentId);
            </td>
        </tr>
        <tr>
            <td>
                <button id="cmdSave" type="submit">Create</button>
            </td>
        </tr>
    </table>
}